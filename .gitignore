# the built binary. Anchored: unanchored, this also matched skills/cinderplot/,
# which silently kept the agent skill -- the source docs/llms.txt is generated
# from -- out of the repository.
/cinderplot
src/*.o
*.pdf
*.png
.DS_Store

# local, uncommitted scratch
TODO.md
CLAUDE.md

# dogfooding drop zone: local intake only, keep just the README tracked
tmp/*
!tmp/README.md
